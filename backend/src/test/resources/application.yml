spring:
  datasource:
    url: jdbc:h2:mem:archly;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
archly:
  google:
    client-id: test-client
  ui-origin: http://localhost:5173
